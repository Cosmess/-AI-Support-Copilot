# Integrations

## Zendesk
- Busca de tickets por query.
- Credenciais via variáveis de ambiente.

## Elasticsearch
- Busca de logs por `message` e `service`.
- Índice padrão: `support-logs`.

## OpenAI
- Embeddings para RAG.
- Chat model para síntese final.
